/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'cdn.shopify.com',
      'bs.plantnet.org',
      'bs.floristic.org'
    ],
  },
}

module.exports = nextConfig 